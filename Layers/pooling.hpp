#ifndef POOLING_HPP
#define POOLING_HPP


class Pooling : public Layers {
    public:
        /** constructor and destructor **/
        Pooling();
        virtual ~Pooling();

    protected:

    private:
};

#endif // POOLING_HPP
